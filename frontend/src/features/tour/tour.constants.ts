/** localStorage: first-run product tour completed (or skipped). */
export const PRODUCT_TOUR_STORAGE_KEY = 'gravity.productTour.v1';

/** Wait for chrome / board-start exit motion before driving. */
export const PRODUCT_TOUR_START_DELAY_MS = 560;

/** Bump when tour steps change so returning users see the updated walkthrough once. */
export const PRODUCT_TOUR_VERSION = '12';
