/** How far rewind / fast-forward jumps along the recorded timeline. */
export const REPLAY_SKIP_MS = 5_000;

/** Brief pause on the empty board before the first event when autoplay starts. */
export const REPLAY_START_HOLD_MS = 450;
