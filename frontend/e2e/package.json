{
  "name": "gravity-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "End-to-end tests for Gravity (Playwright, multi-context collaboration)",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0"
  }
}
